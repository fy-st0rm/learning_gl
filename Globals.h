#pragma once

#include <array>

// Definations
#define GL_Texture std::array<unsigned int, 3>

const static int WIN_WIDTH = 800;
const static int WIN_HEIGHT = 600;

static int ZOOM_WIDTH = 400;
static int ZOOM_HEIGHT = 300;
